import {Meta} from '@storybook/addon-docs/blocks';

import {
    DesignSystem,
    Structure,
    Patterns,
    getStoryTitle
} from 'src/structure';

import {headerImage} from './static';

export const title = getStoryTitle([
    Structure.DESIGN_SYSTEM,
    DesignSystem.PATTERNS,
    Patterns.ACCESSIBILITY
]);

<Meta title={title} />

<br />

<img src={headerImage} />

Наверняка здесь что-то будет...
