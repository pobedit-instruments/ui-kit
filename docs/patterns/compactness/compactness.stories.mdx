import {Meta} from '@storybook/addon-docs/blocks';
import {Typography} from 'src/components';

import {
    DesignSystem,
    Structure,
    Patterns,
    getStoryTitle
} from 'src/structure';

import  {
    stepsImage,
    tableImage,
    tabsImage
} from './static';

export const title = getStoryTitle([
    Structure.DESIGN_SYSTEM,
    DesignSystem.PATTERNS,
    Patterns.COMPACTNESS
]);

<Meta title={title} />

<Typography.Title>{Patterns.COMPACTNESS}</Typography.Title>

Скрывайте детали до тех пор, пока они не понадобятся. Это позволит сохранить место на странице для общей обзорной информации.

Пример вложенности в таблицах:

<img src={tableImage} width={600} />

<br />

Пример c вкладками:

<img src={tabsImage} width={600} />

<br />

Пример с шагами:

<img src={stepsImage} width={600} />
