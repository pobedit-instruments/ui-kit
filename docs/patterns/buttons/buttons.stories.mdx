import {Meta} from '@storybook/addon-docs/blocks';
import {Typography} from 'src/components';

import {
    DesignSystem,
    Structure,
    Patterns,
    getStoryTitle
} from 'src/structure';

import {
    actionImage,
    bodyImage,
    complexImage,
    contrastImage,
    copywritingImage,
    cornersImage,
    badGroupImage,
    goodGroupImage,
    popupImage
} from './static';

export const title = getStoryTitle([
    Structure.DESIGN_SYSTEM,
    DesignSystem.PATTERNS,
    Patterns.BUTTONS
]);

<Meta title={title} />

<Typography.Title>{Patterns.BUTTONS}</Typography.Title>

<br />

1. [Контрастность](#contrast)
2. [Расположение](#position)
3. [Группировка](#grouping)
4. [Акцентные кнопки](#action)
5. [Копирайтинг](#copywriting)
6. [Модальные окна](#popup)

<br />

## Контрастность <a name="contrast"></a>

Кнопки, которые не подразумевают принятия решений (Применить, Отказаться и синонимичные им названия) не должны выделяться цветом, так же как и не должно быть больше одной главной кнопки.

<img src={contrastImage} width={600} />

## Расположение <a name="position"></a>

Пример: расположение кнопок в контеной части

<img src={bodyImage} width={600} />

Пример: расположение кнопок в нижней и верхней частях страницы

<img src={cornersImage} width={600} />

Пример: совмещенное расположения кнопок

<img src={complexImage} width={300} />

## Группировка <a name="grouping"></a>

Пример: желательная группировка кнопок

<img src={goodGroupImage} width={600} />

Пример: нежелательная группировка кнопок

<img src={badGroupImage} width={600} />

## Группировка <a name="action"></a>

Пример: акцент на основное действие

<img src={actionImage} width={600} />

## Копирайтинг <a name="copywriting"></a>

Используйте лаконичные глагольные названия

<img src={copywritingImage} width={600} />

## Модальные окна <a name="popup"></a>

Заголовок должен иметь связь с кнопками

<img src={popupImage} width={600} />
