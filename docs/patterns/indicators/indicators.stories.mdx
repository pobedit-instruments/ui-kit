import {Meta} from '@storybook/addon-docs/blocks';
import {Typography} from 'src/components';

import {
    DesignSystem,
    Structure,
    Patterns,
    getStoryTitle
} from 'src/structure';

import {
    refreshImage,
    skeletonImage,
    spinnerImage
} from './static';

export const title = getStoryTitle([
    Structure.DESIGN_SYSTEM,
    DesignSystem.PATTERNS,
    Patterns.INDICATORS
]);

<Meta title={title} />

<Typography.Title>{Patterns.INDICATORS}</Typography.Title>

Индикаторы выполнения поддерживают диалог с пользователем, когда остальная часть интерфейса недоступна.

Пример спиннера:

<img src={spinnerImage} width={600} />

<br />

Пример скелетона:

<img src={skeletonImage} width={600} />

<br />

Пример с рефрешем (активация по действию):

<img src={refreshImage} width={600} />

<br />

