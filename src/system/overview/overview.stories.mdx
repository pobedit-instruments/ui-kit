import {Meta} from '@storybook/addon-docs/blocks';
import {Typography} from 'src/components';

import {
    DesignSystem,
    Structure,
    getStoryTitle
} from 'src/structure';

export const title = getStoryTitle([
    Structure.DESIGN_SYSTEM,
    DesignSystem.OVERVIEW
]);

<Meta title={title} />

<Typography.Title>{DesignSystem.OVERVIEW}</Typography.Title>

<br />
<br />

<img src="./stories/system/overview/header.png" />

С этим набор компонентов, жизнь разработчика станет чуть длиннее (Пушкин А.С.).

## Преамбула

<br />
Кто мне пришлет ее портрет,<br />
Черты волшебницы прекрасной?<br />
Талантов обожатель страстный,<br />
Я прежде был ее поэт.<br />
С досады, может быть, неправой,<br />
Когда одна в дыму кадил<br />
Красавица блистала славой.<br />
...

(Пушкин А.С.)

## Установка

```
yarn add -D @pushkin/ui-kit
```

## Пример использования

```tsx
import React from 'react';
import {render} from 'react-dom';
import {DatePicker} from '@pushkin/ui-kit';

const App = () => {
    const handleChange = (value: string) => {
      console.log(value);
    };

    return <DatePicker onChange={handleChange} />;
};

render(<App />, document.getElementById('root'));
```

## Тесты

```
npm test
```

## Публикация

```
npm publish --access public --verbose
```
