import {Meta} from '@storybook/addon-docs/blocks';
import {Typography} from 'antd';

import {
    DesignSystem,
    Structure,
    Patterns,
    getStoryTitle
} from 'src/structure';

export const title = getStoryTitle([
    Structure.DESIGN_SYSTEM,
    DesignSystem.PATTERNS,
    Patterns.COMPACTNESS
]);

<Meta title={title} />

<Typography.Title>{Patterns.COMPACTNESS}</Typography.Title>

Скрывайте детали до тех пор, пока они не понадобятся. Это позволит сохранить место на странице для общей обзорной информации.

Пример вложенности в таблицах:

<img src="./stories/system/patterns/compactness/table.png" width={600} />

<br />

Пример c вкладками:

<img src="./stories/system/patterns/compactness/tabs.png" width={600} />

<br />

Пример c шагами:

<img src="./stories/system/patterns/compactness/steps.png" width={600} />
